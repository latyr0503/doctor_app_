import 'package:flutter/material.dart';

class Specialiste extends StatelessWidget {
  const Specialiste({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Onglet Specialiste');
  }
}