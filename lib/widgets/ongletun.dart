import 'package:flutter/material.dart';

class Traitement extends StatelessWidget {
  const Traitement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('L`\'onglet Traitement');
  }
}